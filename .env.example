# URL del SAP B1 backend (por defecto localhost:4100)
NEXT_PUBLIC_BACKEND_URL=http://localhost:4100

# API key del tenant (si se define, salta el login)
# Copiar el valor de TAMAPRINT_API_KEY o FLEXOIMPRESOS_API_KEY del backend
NEXT_PUBLIC_SAP_API_KEY=
